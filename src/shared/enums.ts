export enum TimePeriod {
    P1Y = "P1Y",
    P1M = "P1M",
    P1D = "P1D",
    PT1H = "PT1H",
    PT15M = "PT15M",
}

export enum Currency {
    EUR = "EUR",
}

export enum ShellyViewType {
    DEVICE = "DEVICE",
    GROUP = "GROUP",
}
